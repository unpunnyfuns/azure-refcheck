# Simple common build template
parameters:
  - name: language
    type: string
    default: 'any'

steps:
  - script: |
      echo "Common build template for validation testing"
      echo "Language: ${{ parameters.language }}"