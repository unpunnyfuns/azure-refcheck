# Simple common test template
parameters:
  - name: language
    type: string
    default: 'any'

steps:
  - script: |
      echo "Common test template for validation testing"
      echo "Language: ${{ parameters.language }}"