# Simple build template (v1)
parameters:
  - name: language
    type: string
    default: 'any'

steps:
  - script: |
      echo "Build template v1 for validation testing"
      echo "Language: ${{ parameters.language }}"